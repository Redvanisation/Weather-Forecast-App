/* eslint-disable no-new */
// import cities from './cities';

// const Suggestions = require('suggestions');


// const suggest = () => {
  
  // return import(/* webpackChunkName: "./cities" */ './cities').then(({ default: cities }) => {
    
    // const name = 'new';
    // const validCities = (name) => cities.includes(name);

  //  return element;
  // }).catch(error => 'An error occurred while loading the component');

    // console.log(validCities('new'));


  // const validCities = (name) => cities.includes(name);

  // const search = document.querySelector('#search');
  // let theCities = [];

  // search.addEventListener('change', () => {
  //   return theCities.push(validCities(search.value));
  // });

  // console.log(theCities);

  // new Suggestions(search, theCities, {
  //   minLength: 3,
  //   limit: 3,
  // });

//   return {
//     validCities,
//   };
// })
// };

// export default suggest;
